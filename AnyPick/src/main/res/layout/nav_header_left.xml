<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="220dp"
    android:background="?attr/colorPrimary"
    >

    <ImageView
        android:id="@+id/left_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <ImageView
        android:id="@+id/user_icon"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_centerInParent="true"/>
    <TextView
        android:id="@+id/username_show"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:layout_below="@id/user_icon"
        android:layout_centerHorizontal="true"
        android:text="username"/>
    <ImageButton
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:id="@+id/remove_website"
        android:layout_alignParentBottom="true"
        android:background="@drawable/ic_remove_circle_outline_white_48dp"/>
    <TextView
        android:id="@+id/remove_website_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:textSize="30dp"
        android:textColor="#ffffff"
        android:text="Delete"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@id/remove_website"/>
</RelativeLayout>